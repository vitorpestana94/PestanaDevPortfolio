export default function HomeFormButton({ label }: { label: string }) {
  return (
    <button
      className="py-2 px-4 rounded-md text-[0.5rem] sm:text-[0.75rem] bg-[#38b6ff]"
      onClick={() => {}}
    >
      {label}
    </button>
  );
}
